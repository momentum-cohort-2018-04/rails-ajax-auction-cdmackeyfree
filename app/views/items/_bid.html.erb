  <div>
    <%= form_with(model: @bid, remote:true) do |f| %>
        <h3>Your Bid <%= @item.title %></h3>
        <%= f.hidden_field :item_id %>
        <%= f.number_field :amount %>
        <%= f.submit "Bid!", class: "btn btn-primary btn-small" %>
    <% end %>
  </div>
